{
  "buildCommand": "cd diseno-espanol && npm install && npm run build",
  "outputDirectory": "diseno-espanol/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
